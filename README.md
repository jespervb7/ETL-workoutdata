# ETL-workoutdata

#MVP
This project will contain a simple ETL proces. It uses the training-session json files from polar-flow.com that users can requests and puts it into simple csv files for now. 

# Nice to haves
Eventually integrate this into a live/local database.